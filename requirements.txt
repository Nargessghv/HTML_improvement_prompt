pandas>=2.0.0
python-pptx>=0.6.21
openpyxl>=3.1.0
python-dotenv>=1.0.0  # For environment variable management

# LLM Integration Options (install one based on preference)
openai>=1.0.0  # For OpenAI GPT integration
anthropic>=0.7.0  # For Anthropic Claude integration

# AI Agents and Workflow Orchestration
langgraph>=0.0.40  # For AI agent workflows and graph orchestration
langchain>=0.1.0  # Core framework for Langgraph
langchain-openai>=0.1.0  # OpenAI integration for Langchain
instructor>=1.0.0  # For structured LLM outputs with Pydantic models

# LLM Monitoring and Observability
langfuse>=2.20.0,<3.0.0  # For LLM call monitoring and analytics (SDK v2)

# Cloud Storage for Media
azure-storage-blob>=12.26.0  # For Azure Blob Storage integration

# HTML Rendering for Visualizations
playwright>=1.40.0  # PRIMARY RENDERER: Most reliable, best quality
# IMPORTANT: After pip install, run: playwright install chromium

# Optional fallback renderers (not needed for most users):
selenium>=4.15.0  # Alternative: Requires Chrome browser
# weasyprint>=60.0  # SKIP THIS: Requires system deps (brew install cairo pango gdk-pixbuf libffi)
# imgkit>=1.2.3  # Alternative: wkhtmltopdf wrapper

# Quarto
quarto>=0.1.0

# HTTP requests
requests>=2.28.0

# SVG to PNG conversion
cairosvg>=2.5.0

# Image processing for DPI metadata
Pillow>=10.0.0

# Document processing
PyPDF2>=3.0.0  # For PDF text extraction
python-docx>=0.8.11  # For Word document processing
# textract>=1.6.5  # Optional: Fallback text extraction for various formats (requires additional system dependencies)

# PPTX to Image Conversion
aspose-slides>=25.7.0  # For PPTX to PNG conversion
pdf2image>=1.16.0  # Alternative PPTX to image via PDF

# Lucide Static Icons (commented out - not available in PyPI)
# lucide-static  # For icon management

# Optional: For local model support
# transformers>=4.21.0
# torch>=2.0.0

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
supabase>=2.0.0
httpx>=0.25.0  # For webhook HTTP requests and async client
aiohttp>=3.9.0  # For async HTTP client in image generation