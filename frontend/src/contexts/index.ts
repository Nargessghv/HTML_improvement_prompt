// React contexts
// Re-export all contexts from this directory

// Example exports (to be implemented):
// export { AuthContext, AuthProvider, useAuth } from './AuthContext'
// export { ThemeContext, ThemeProvider, useTheme } from './ThemeContext'
// export { NotificationContext, NotificationProvider, useNotification } from './NotificationContext'

// Placeholder - remove when actual contexts are added
export const contexts = {
  // React contexts will be exported here
}